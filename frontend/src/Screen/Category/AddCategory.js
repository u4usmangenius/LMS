import React from "react";
import { observer } from "mobx-react-lite";

const AddCategory = () => {
  return (
    <>
      <h1>AddCategory</h1>
    </>
  );
};

export default observer(AddCategory);
